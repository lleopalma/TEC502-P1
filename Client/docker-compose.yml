services:
  operador:
    build: ./
    command: python -u client.py
    container_name: "operador"
    stdin_open: true
    tty: true
    environment:
      - SERVER_HOST=${SERVER_HOST:-servidor}
    networks:
      - iot_net

networks:
  iot_net:
    external: true
    name: iot_net